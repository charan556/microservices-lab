management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: '8181'
spring:
  application:
    name: catalogservice
  cloud:
    config:
      uri: http://localhost:8001
  profiles:
    active: default